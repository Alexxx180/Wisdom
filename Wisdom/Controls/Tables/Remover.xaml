<UserControl x:Class="Wisdom.Controls.Tables.Remover"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="800"
             x:Name="This">
    <Grid Style="{StaticResource Light}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="35" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Height="35" Style="{StaticResource DeleteButton}" VerticalAlignment="Top"/>
        <ContentControl Grid.Column="1" Template="{Binding ElementName=This, Path=Item}"/>
    </Grid>
</UserControl>
