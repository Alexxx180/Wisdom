<UserControl x:Class="Wisdom.Controls.Tables.ThemePlan.Themes.Works.PlanWork"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tasks="clr-namespace:Wisdom.Controls.Tables.ThemePlan.Themes.Works.Tasks"
             mc:Ignorable="d" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="105" d:DesignWidth="800" x:Name="This">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.05*" />
                <ColumnDefinition Width="0.9*" />
            </Grid.ColumnDefinitions>

            <Button Click="DropRecord">
                <Button.Style>
                    <StaticResource ResourceKey="DeleteButton"/>
                </Button.Style>
            </Button>
            <TextBlock Grid.Column="1">
                <TextBlock.Style>
                    <StaticResource ResourceKey="RegularSections"/>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <Grid Grid.Row="1" Style="{StaticResource ListCaptions2}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.05*" />
                <ColumnDefinition Width="0.15*" />
                <ColumnDefinition Width="0.7*" />
                <ColumnDefinition Width="0.1*" />
            </Grid.ColumnDefinitions>

            <Viewbox Grid.Column="1" HorizontalAlignment="Left">
                <TextBlock Text="№"/>
            </Viewbox>

            <Viewbox Grid.Column="2" HorizontalAlignment="Left">
                <TextBlock Text="Описание"/>
            </Viewbox>

            <Viewbox Grid.Column="3" HorizontalAlignment="Right">
                <TextBlock Text="Часы"/>
            </Viewbox>
        </Grid>

        <StackPanel Grid.Row="2" Background="#FFEEEBB6">
            <ItemsControl>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemsSource>
                    <Binding Path="Tasks"/>
                </ItemsControl.ItemsSource>
            </ItemsControl>

            <tasks:PlanTaskAdditor x:Name="TaskAdditor">
                <tasks:PlanTaskAdditor.Work>
                    <Binding ElementName="This"/>
                </tasks:PlanTaskAdditor.Work>
            </tasks:PlanTaskAdditor>
        </StackPanel>
    </Grid>
</UserControl>