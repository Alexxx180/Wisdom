<UserControl x:Class="Wisdom.Controls.Forms.MainForm.UserTemplates.TemplateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="45" d:DesignWidth="1000">
    <Button HorizontalContentAlignment="Left" Click="CreateFromTemplate">
        <Button.Style>
            <StaticResource ResourceKey="ListCaptions"/>
        </Button.Style>
        <Button.Content>
            <TextBlock Text="{Binding Path=FileName}" />
        </Button.Content>
    </Button>
</UserControl>